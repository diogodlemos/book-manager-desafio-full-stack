# book-manager-desafio-full-stack
Para rodar o backend com docker execute o comando na raiz do projeto:
docker compose --env-file .env up --build

ou testar em prod:
docker compose --env-file .env.production up --build